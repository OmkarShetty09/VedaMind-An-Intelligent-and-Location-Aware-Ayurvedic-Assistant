# Placeholder guard: the RAG service must only be reachable by the backend
# over the Docker network, never through nginx.
location /api/v1/chat/ {
  proxy_pass http://backend:8000;
  proxy_buffering off;
}